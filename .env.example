# 高德 Web 服务 API Key
# 真实 Key 只能填写在本地 .env 或 GitHub Secrets 中，不能写死在代码里。
AMAP_KEY=请填写你的高德Web服务API Key

# 前端浏览器定位用的高德 Key。
# 这个 Key 会进入生成后的网页，请在高德后台限制域名白名单。
AMAP_BROWSER_KEY=请填写允许当前网站域名调用的高德Web服务API Key

# 默认城市：广州
DEFAULT_ADCODE=440100
DEFAULT_CITY=广州
DEFAULT_LATITUDE=23.1291
DEFAULT_LONGITUDE=113.2644
SITE_NAME=天气预报

# 企业微信群机器人 Webhook 地址。
# 真实地址只能放在 .env 或 GitHub Secrets 中，不能提交到公开仓库。
WECOM_WEBHOOK_URL=

# GitHub Pages 网站地址，用于企业微信消息里的查看链接。
SITE_URL=https://dragonzero123.github.io/weather-site/

# 隐私说明：
# 用户经纬度和详细位置只在浏览器当前页面临时使用。
# 用户位置不会写入 data/weather.json、logs/update-log.md，也不会发送到企业微信群。
